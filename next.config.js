/** @type {import('next').NextConfig} */
const nextConfig = {
    // NOTE: If you are using Next.js 13.4.x or older, you need to add the `experimental` flag.
    // experimental: {
    //   serverActions: true,
    // },
    // output: 'standalone',
    // reactStrictMode: false,
};

module.exports = nextConfig;

    